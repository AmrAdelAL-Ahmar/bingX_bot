# دليل استخدام وتطوير بوت تداول BingX عبر التيليجرام 🤖📈

إليك الدليل الشامل لآلية عمل بوت التداول الخاص بك على منصة BingX، بالإضافة إلى أبرز مميزاته، الشروط اللازمة لعمله، وطريقة استخدامه، مع خطة للتطوير المستقبلي.

---

## ⚙️ أولاً: كيف يعمل البوت؟
البوت عبارة عن نظام تداول آلي (Auto-Trading) متكامل مع تطبيق **Telegram** ومنصة **BingX Futures**. وتتلخص آلية عمله في الخطوات التالية:

1. **استقبال الإشارات (Signals):** يقوم البوت بمراقبة مجموعة أو قناة تيليجرام محددة مسبقاً، وفور وصول إشارة تداول (توصية) يقرأها ويحللها فوراً.
2. **تحليل البيانات (Parsing):** يستخرج البوت من نص الإشارة أهم المعطيات: العملة (Coin)، اتجاه الصفقة (Long/Short)، مناطق الدخول (Entry)، أهداف جني الأرباح (Take Profits)، ووقف الخسارة (Stop Loss).
3. **التخطيط وإدارة المخاطر:** يعتمد البوت على إعداداتك المسبقة لحساب حجم الدخول والرافعة المالية المناسبة (Leverage) بناءً على نسبة المخاطرة المحددة لكل صفقة.
4. **التنفيذ التلقائي:** يرسل البوت أوامر التداول (فتح الصفقة مع تحديد أهداف الربح ومستوى وقف الخسارة) مباشرة إلى حسابك في BingX عبر الـ API.
5. **المتابعة (Position Monitoring):** يراقب البوت الصفقة أثناء تفعيلها، ويتخذ إجراءات تلقائية مثل تحريك وقف الخسارة (Trailing/Break-even) لحماية الأرباح.

---

## 🌟 ثانياً: أبرز المميزات (Features)
1. **تداول آلي وسريع:** تنفيذ صفقات العقود الآجلة (Futures) في أجزاء من الثانية بمجرد رصد الإشارة.
2. **إدارة متقدمة للمخاطر (Risk Management):**
   - **حد أقصى للمخاطرة لكل صفقة:** يمنع البوت المخاطرة بأكثر من 5% من رأس المال في الصفقة الواحدة.
   - **حد أقصى للتعرض (Total Exposure):** يمنع فتح صفقات جديدة إذا تجاوز إجمالي المخاطرة في السوق 10% من رأس المال.
   - **الحد من الخسارة القصوى:** يضمن ألا يتجاوز أقصى خسارة محتملة من الـ Stop Loss نسبة 6% عبر تعديل حجم الدخول والرافعة المالية تلقائياً.
3. **تأمين الأرباح الديناميكي:** عند وصول الصفقة للهدف الأول (TP1)، يقوم البوت تلقائياً بتعديل مستوى وقف الخسارة إلى منطقة الدخول (Break-even) لحماية رأس المال من تقلبات السوق المفاجئة.
4. **إعدادات قابلة للتخصيص عبر التيليجرام:** يتيح لك البوت ضبط نسبة المخاطرة، وأحجام التداول، والرافعة المالية الافتراضية بسهولة باستخدام الأوامر.
5. **تقارير دورية دقيقة:** يمكنك طلب تقارير تفصيلية لأداء البوت (أرباح/خسائر ومعدل نجاح الصفقات Win Rate) بشكل أسبوعي أو شهري.
6. **تخزين آمن للبيانات:** يعتمد البوت على قاعدة بيانات **MongoDB** لحفظ سجلات الصفقات والإشارات بدقة للرجوع إليها.

---

## 📋 ثالثاً: شروط ومتطلبات التشغيل (Requirements)
لكي يعمل البوت بأعلى كفاءة، يجب توافر الشروط التالية:
1. **مفاتيح API من منصة BingX:** مفاتيح (API Key + Secret Key) مفعلة مع صلاحيات التداول في العقود الآجلة (Futures Trading Enabled).
2. **عناصر التيليجرام:** 
   - **Bot Token:** مستخرج من (BotFather).
   - **تصريح الدخول:** يجب إضافة البوت إلى مجموعة الإشارات كـ "مسؤول" (Admin) أو منحه صلاحية قراءة الرسائل، وتوفير معرف المجموعة (Chat ID).
3. **قاعدة بيانات (Database):** اتصال نشط بقاعدة بيانات MongoDB لتسجيل المعاملات وحفظ التقارير.
4. **صيغة الإشارات (Signal Format):** يشترط كتابة التوصيات (مناطق الدخول، الأهداف، الوقف) بصيغة واضحة ومفهومة لمحلل البوت (Parser).
5. **بيئة استضافة (Hosting):** خادم (Node.js) متصل بالإنترنت على مدار الساعة لضمان استمرارية العمل.

---

## 🎮 رابعاً: طريقة الاستخدام والأوامر (Usage & Commands)
بعد تشغيل البوت وربطه بحسابك، يمكنك التحكم به من خلال محادثة البوت على تيليجرام باستخدام الأوامر التالية:

- `/help` أو `/start`: يعرض رسالة ترحيبية وقائمة بالأوامر المتاحة ووظيفة كل أمر.
- `/status`: للتحقق من حالة البوت (هل هو متصل بالمنصة ويعمل بشكل سليم؟) وعرض الرصيد الحالي المتاح للتداول.
- `/settings`: لضبط إعدادات التداول (مثل نسبة المخاطرة الافتراضية، أو الرافعة المالية).
- `/report`: لطلب تقرير أداء البوت (يعرض لك الأرباح، الخسائر، ومعدل نجاح الصفقات).
- `/positions`: لعرض الصفقات المفتوحة حالياً ومقدار الربح/الخسارة غير المحقق (PNL).

**طريقة الاستخدام العملية:**
1. قم بضبط الإعدادات الخاصة بك عبر الأمر `/settings`.
2. تأكد من أن حالة البوت تتصل بشكل صحيح عن طريق `/status`.
3. بمجرد نشر إشارة في مجموعة التيليجرام المراقبة، سيقوم البوت بقراءتها والتداول نيابة عنك بناءً على الإعدادات.
4. يمكنك متابعة النتائج بشكل دوري عبر طلب التقارير `/report`.

---

## 🚀 خامساً: خطة التطوير المستقبلية (Future Roadmap)
لزيادة سهولة الاستخدام واحترافية البوت، تم وضع خطة التطوير التالية التي يمكنا تنفيذها قريباً:

### 1. قائمة أوامر تفاعلية للتيليجرام (Bot Menu & Shortcuts)
- **إنشاء قائمة أزرار (Inline/Reply Keyboard)** في محادثة التيليجرام بدلاً من كتابة الأوامر يدوياً.
- عند تشغيل الأمر `/menu` أو `/start`، ستظهر أزرار سريعة ومختصرة مثل:
  - 📊 **طلب تقرير الأداء (Weekly/Monthly)**.
  - ⚙️ **إعدادات البوت**.
  - 💼 **حالة الصفقات المفتوحة**.

### 2. تفعيل وإلغاء ميزة التحكم الصارم بالخسارة ديناميكياً عبر الواجهة
- إضافة زر أو أمر مخصص للتحكم السريع في متغير `ENFORCE_MAX_SL_LOSS`.
- **الهدف:** السماح للمستخدم بـ **تفعيل (Enable)** أو **إلغاء (Disable)** ميزة الحد الأقصى للخسارة (كـ 6% من الرصيد) بضغطة زر من داخل التيليجرام دون الحاجة لإعادة تشغيل الكود أو تعديل ملف `.env`.
- سيتم عرض الحالة الحالية لإدارة المخاطر (مفعل/غير مفعل) ضمن رسالة الإعدادات لتكون واضحة ومحدثة باستمرار.

### 3. إشعارات ذكية ومخصصة
- عند تفعيل أو تعطيل أي إعداد من إعدادات المخاطر عبر القائمة، سيقوم البوت بإرسال إشعار فوري يؤكد نجاح العملية وحالة النظام.
- تحسين الإشعارات التي يرسلها البوت لتشمل رسائل ملخصة ومنسقة بعد كل إغلاق لصفقة (محتوية على الربح/الخسارة المحققة).
