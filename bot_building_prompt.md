# هندسة موجه الأوامر (Prompt Engineering) لإنشاء بوت تداول مخصص
*يمكنك استخدام هذا الموجه (Prompt) من خلال نسخه ولصقه لأي نموذج ذكاء اصطناعي (مثل ChatGPT أو Claude أو Gemini) لطلب برمجة بوت تداول احترافي لأي منصة أخرى (مثل Binance, Bybit, OKX).*

***

**[انسخ النص أدناه وابدأ به المحادثة]**

أريد بناء بوت تداول آلي للعملات الرقمية متكامل وقوي يعمل على منصة [اسم المنصة، مثلاً: Binance أـو Bybit] باستخدام بيئة Node.js ولغة TypeScript. سيدار هذا البوت عبر واجهة تطبيق Telegram (باستخدام مكتبة Telegraf)، ويجب أن تكون قاعدة بياناته MongoDB لتخزين الصفقات النشطة وبيانات المستخدمين.

أطالبك بالالتزام بالهيكلية المعمارية التالية والقيود الموضحة أدناه للخروج بكود نظيف (Clean Code)، قابل للتوسع والصيانة ويتوافق مع مبادئ (SOLID):

### 1. هندسة وهيكل المشروع (Architecture):
- تطبيق نمط SOA (Service Oriented Architecture) لتقسيم العمليات.
- **`ExchangeService`**: فئة مسؤولة حصرياً ومستقلة عن الاتصال بمنصة [اسم المنصة] بالاعتماد التام على مكتبة `ccxt`. يجب أن تدعم دوال: فتح الأوامر (دخول السوق المباشر أو المعلق، تحديد SL و TP)، جلب الرصيد والتأكد من الأرصدة المتاحة للـ Futures، جلب الصفقات المفتوحة بالتفصيل، وتغيير نسبة الـ Leverage، والانتقال التلقائي للـ Margin Mode.
- **`TradeManager`**: هذه الخدمة ستكون بمثابة عقل البوت لمعالجة طلبات فتح/إغلاق الصفقات، وحساب أحجام الصفقات (Position Sizing) الدقيقة قبل الشروع في التداول بناءً على نسبة خطورة (Risk %) يحددها المستخدم.
- **`SignalParser`**: خدمة متخصصة في استخراج أوامر التداول من نصوص ورسائل التليجرام الخام وتحويلها إلى كائنات برمجية مهيكلة بذكاء (Symbol, Entry, Targets, Stop Loss, Leverage) مستندة لتعابير نمطية متينة (Regex).
- **`PositionMonitor`**: خدمة تعمل في الخلفية بآلية (Polling) لمراقبة الصفقات المفتوحة دورياً. وظيفتها تحريك الـ Stop Loss لنقطة الدخول (Break-Even) بعد تحقيق الهدف الأول تلقائياً لحماية الأرباح، وإرسال تنبيهات استباقية (Warnings) إذا وصلت الخسارة لنسبة مئوية حرجة من الحساب، وتحديث وإسقاط حالة الصفقات المغلقة في قاعدة البيانات بعد تزامنها مع المنصة.
- **الفصل التام للرؤية (Separation of Concerns)**: يمنع كتابة دوال الأزرار والـ Logic داخل ملف `index.ts`. يجب أن يتم تعريف أوامر التليجرام وردود الأفعال (Middlewares, Action Handlers, Hears) وتجزئتها داخل مجلد مخصص للمتحكمات كالتالي `src/bot/handlers`.

### 2. إدارة المخاطر وأمان المحفظة (Risk Management):
- عند محاولة فتح صفقة، يجب على البوت حساب كمية العقود بحيث لا تتجاوز المخاطرة (رأس المال المفترض خسارته عند ضرب الوقف) نسبة $X% من إجمالي الحساب المتاح.
- إضافة ميزة لمنع النظام من فتح الصفقات إذا طغى الانكشاف الكلي للصفقات (Total Margin Used) على 10% إلى 15% من رأس المال لمنع تصفية المحفظة بأكملها بسبب تقلبات السوق.

### 3. واجهة المستخدم على التيليجرام (Telegram Interface):
- توفير قائمة أزرار سفلية (Reply Keyboard) مريحة وأخرى مضمنة (Inline Keyboards) لإدارة إعدادات تفضيلات التنبيهات.
- توفير دوال للاستجابة وعرض: رصيد المحفظة والأرباح العائمة، سرد الصفقات المفتوحة بالتفصيل ونسب الأرباح اللحظية، طلب تقارير للأداء السابق مع بيان معدل النجاح (Win Rate). توفير قدرات كافية لإغلاق صفقات محددة أو كافة الأرصدة دفعة واحدة بأسعار السوق فورياً (Panic Close).
- إلزام الحماية للبوت عبر إعداد "القائمة البيضاء" (Whitelist) لحماية الحساب ومنع المستخدمين الخارجيين من اصدار أوامر في التليجرام.

### 4. المتطلبات والأسس التقنية:
- كتابة الكود باستخدام TypeScript بالكامل، واعتماد الأنواع القوية الصريحة (Interfaces/Types) وتجنب استخدام المتغير العائم `any`.
- المعالجة القوية للأخطاء (Robust Error Handling) لاسيما في مواجهة تجاوزات الـ Rate Limit، انقطاعات الشبكة، والكميات غير المتاحة، والرد بأخطاء واضحة للمستخدم في التليجرام وتدوين الباقي في ملف Logs عبر (Winston أو ما شابه).

**الخطوة الأولى لك كنموذج:** 
يرجى دراسة طلبي هذا بعناية وإعطائي خطة التنفيذ والهيكلة الخاصة بكافّة المجلدات المترقبة وقائمة المكتبات المراد تحميلها أولاً، وانتظر مني الموافقة للبدء بتوليد الأكواد تباعاً.
