# تقرير بنية وعمل بوت التداول (BingX Trading Bot)

## 1. نظرة عامة
هذا البوت هو نظام تداول آلي يعتمد على منصة Telegram للتفاعل مع المستخدم ومنصة BingX لتنفيذ الصفقات في العقود الآجلة (Futures). البوت مبني باستخدام بيئة Node.js و TypeScript، ويستخدم مكتبة `telegraf` لإدارة بوت تيليجرام، و `ccxt` للتواصل مع واجهة برمجة التطبيقات (API) المستقلة الخاصة بـ BingX، بالإضافة إلى `mongoose` لإدارة قواعد البيانات وتخزين معلومات المستخدم وتسجيل الصفقات.

## 2. هيكلية المشروع (Architecture)
يتبع المشروع نمط التصميم الخدمي (Service-Oriented Architecture) حيث يتم تقسيم المنطق البرمجي إلى عدة خدمات رئيسية متخصصة ومستقلة:

*   **نقطة الدخول وملف التيليجرام الأساسي (`index.ts`):** 
    يدير إعدادات بوت التيليجرام بشكل كامل (تكوين `Telegraf`)، ويستقبل أوامر المستخدم (نصوص، أزرار، أوامر)، ويعالج التحقق من صلاحيات المستخدم (Middleware كالقائمة البيضاء للمعرفات). يضم هذا الملف حالياً جميع دوال الاستجابة (Bot Hears & Commands) ومنطق ترتيب وعرض واجهات المستخدم السفلية والرسائل التفاعلية (يحتاج لعملية File Splitting وتنظيف كجزء من الـ Refactoring القادم).
*   **خدمة الاتصال بالمنصة (`BingXService`):** 
    مسؤولة حصرياً عن جميع التفاعلات مع منصة BingX عبر الاستفادة من مكتبة `ccxt`. مهامها واضحة ومحددة: الاستعلام عن الرصيد، جلب الصفقات المفتوحة، تعديل الرافعة المالية (Leverage)، تغيير وضع الهامش المعزول أو المتبادل، وطلب فتح الصفقات برمجياً (السوق أو الأوامر المعلقة).
*   **عقل التداول والمنطق (`TradeManager`):** 
    تمثل الطبقة الوسطى والعصب الرئيسي لتمرير الصفقات. أبرز مسؤولياتها:
    *   التحقق من الرصيد المتوفر في المحفظة.
    *   حساب حجم الصفقة الدقيق (Position Sizing) بناءً على نسبة المخاطرة المطلوبة وأدنى حد للتداول في المنصة.
    *   **تطبيق قواعد إدارة رأس المال القاسية:** يضمن عدم تجاوز إجمالي الهامش المخصص للصفقات المستمرة نسبة معينة من المحفظة (مثلاً 10%). وفي حال تفعيل خطة حماية رأس المال، يتم التأكد من أن أسوأ سيناريو لخسارة الصفقة عند ضرب (Stop Loss) لن تتجاوز الـ 6% من الحساب. يقوم بتقليص حجم العقود (Contracts) تلقائياً لتحقيق وتثبيت نسبة الأمان المطلوبة.
    *   الإشراف على إرسال طلب الصفقة لـ `BingXService`، وتخزين بيانات العملية بنجاح في قاعدة بيانات MongoDB مع كل التفاصيل لحين متابعتها.
*   **محلل الإشارات النصية (`SignalParser`):** 
    يقوم بتحليل النصوص والأوامر الواردة للبوت واستخراج البيانات الاستثمارية بذكاء باستخدام التعابير النمطية (Regex). يتم تحويل الرسائل النصية غير المنظمة إلى كائن مهيكل (رمز العملة Symbol، الاتجاه المالي LONG/SHORT، مناطق الدخول، الأهداف المتعاقبة، وقف الخسارة، ونسبة الرافعة المالية). يمتلك مرونة في تحليل صيغ ولغات متعددة للإشارات.
*   **מراقب الصفقات النشط (`PositionMonitor`):** 
    خدمة منفصلة تعمل في الخلفية بأسلوب (Polling) بحيث تقوم بعملية دوران دورية (كل 30 ثانية). مهامها أساسية لحماية الأرباح:
    *   تطابق بيانات الصفقات المحفوظة محلياً مع السجلات الخاصة في منصة BingX الحية.
    *   **تحريك نقطة التعادل (Break-Even):** نقل وقف الخسارة (SL) فوراً إلى نقطة الدخول بمجرد ملاحظة اصطدام السعر بالهدف الأول (TP1).
    *   إرسال تحذيرات ذكية למستخدم عبر تيليجرام إذا تجاوزت الخسارة العائمة في إحدى الصفقات 5% من مجمل الرصيد الأصلي.
    *   إشعار المستخدم عند اقتراب السعر بنسبة مئوية كبيرة من أهدافه (مثال 70% أو 90% مسافة نحو الهدف).
    *   تحديث وإقفال الصفقات المنتهية (من ضرب الهدف أو الوقف) في السجلات المحلية وإعلام السيرفر بذلك دون إزعاج المجموعات.
*   **خدمة التقارير الدورية (`ReportingService`):** 
    تستخدم أوامر مجدولة (`node-cron`) لطباعة وإنشاء تقارير زمنية للعمليات التجارية والإنجاز (كالتشغيل الأسبوعي أو كل بداية شهر) مع تلخيص عدد الصفقات، نسبة النجاح الاجمالية وحجم الربح والخسارة.

## 3. آلية وتدفق العمل (Workflow)
1. **الاستقبال:** يقوم المستخدم بإرسال رسالة نصية تشتمل على إشارة تداول إلى بوت التيليجرام.
2. **الترجمة والفهم:** يقوم ملف `index.ts` بتخطي الـ (Middlewares) وتمرير النص لخدمة `SignalParser` الذي يترجم المحتوى لقيم ملموسة (بيانات الصفقة).
3. **التخطيط والتنفيذ:** تستلم `TradeManager` المعطيات، ترسم الحجم المنطقي والمسموح به للصفقة بالاعتماد على رصيد المحفظة الحالي وضوابط المخاطرة، ومن ثم تنفذ الطلب بالربط عن طريق `BingXService` وتسجلها في قاعدة البيانات.
4. **المراقبة:** تتولى خدمة `PositionMonitor` زمام الأمور؛ حيث تبقى على اطلاع دائم لحركة الصفقة مع تعديل الأوامر للحماية عند تحقيق الهدف الأول وإشعار المستخدم.
5. **لوحة التحكم:** يمكن للمستخدم دائماً التدخل بإجراء الاستعلامات من أزرار التيليجرام الرئيسية لعرض محفظته الصافية، وتتبع تفاصيل الأداء، أو حتى إلغاء العقود برمجياً بضغطة زر.
